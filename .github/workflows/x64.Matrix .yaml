name: OpenWrt x64 Matrix
permissions: write-all

on:
  workflow_dispatch:
    inputs:
      ssh:
        description: 'Enable SSH debugging'
        required: false
        default: 'false'
  schedule:
    - cron: '0 18 * * 5'

env:
  REPO_URL: https://github.com/openwrt/openwrt
  TZ: Asia/Shanghai
  UPLOAD_BIN_DIR: false
  UPLOAD_FIRMWARE: true
  UPLOAD_RELEASE: true
  GITHUB_RELEASE: https://g3s0.short.gy/OpenWRT
  KERNEL_PATCHVER: 无内核，纯LXC ROOTFS
  PRODUCT_NAME: X86_X64_Matrix

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        target:
          - name: openwrt24
            branch: openwrt-24.10
            config: OpenWRT/x86_x64/v23.x64.mini.config
            file_name: OP2410_X64_ROOTFS_mini
            version: 24.10
          - name: openwrt23
            branch: openwrt-23.05
            config: OpenWRT/x86_x64/v23.x64.mini.config
            file_name: OP2305_X64_ROOTFS_mini
            version: 23.05
          - name: openwrt22
            branch: openwrt-22.03
            config: OpenWRT/x86_x64/v23.x64.mini.config
            file_name: OP2203_X64_ROOTFS_mini
            version: 22.03
    env:
      FILE_NAME: ${{ matrix.target.file_name }}
      CONFIG_FILE: ${{ matrix.target.config }}
      DISTRIB_REVISION: OpenWRT ${{ matrix.target.version }}

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup build environment
        run: |
          sudo rm -rf /etc/apt/sources.list.d/*
          sudo timedatectl set-timezone "$TZ"
          sudo mkdir -p /mnt/workdir
          sudo chown $USER:$GROUPS /mnt/workdir
          ln -sfn /mnt/workdir $GITHUB_WORKSPACE/workdir

      - name: Clone source code
        working-directory: ${{ github.workspace }}/workdir
        run: |
          git clone $REPO_URL -b ${{ matrix.target.branch }} openwrt
          cd openwrt
          useVersionInfo=$(git show -s --date=short --format="Author: %an\nDate: %cd\nCommit: %s\nCommit hash: %H")
          echo "DATE=$(date '+%Y-%m-%d %H')" >> $GITHUB_ENV
          echo "DATE1=$(date '+%Y-%m-%d')" >> $GITHUB_ENV
          echo "FILE_DATE2=$(date '+%Y%m%d%H%M')" >> $GITHUB_ENV
          echo "OpenWRT v${{ matrix.target.version }} 更新记录\n$useVersionInfo" > $GITHUB_WORKSPACE/${{ matrix.target.branch }}.version.txt

      - name: Update feeds
        run: cd ${{ github.workspace }}/workdir/openwrt && ./scripts/feeds update -a

      - name: Install feeds
        run: cd ${{ github.workspace }}/workdir/openwrt && ./scripts/feeds install -a

      - name: Load custom config
        run: |
          [ -e files ] && mv files ${{ github.workspace }}/workdir/openwrt/files
          [ -e banner ] && mv banner ${{ github.workspace }}/workdir/openwrt/package/base-files/files/etc/banner
          [ -e $CONFIG_FILE ] && mv $CONFIG_FILE ${{ github.workspace }}/workdir/openwrt/.config
          chmod +x OpenWRT/custom.sh
          cd ${{ github.workspace }}/workdir/openwrt
          $GITHUB_WORKSPACE/OpenWRT/custom.sh

      - name: SSH connection to Actions (optional)
        if: github.event.inputs.ssh == 'true'
        uses: P3TERX/ssh2actions@v1.0.0
        env:
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}

      - name: Download packages
        run: |
          cd ${{ github.workspace }}/workdir/openwrt
          make defconfig
          make download -j$(nproc)
          find dl -size -1024c -exec rm -f {} \;

      - name: Compile firmware
        id: compile
        run: |
          cd ${{ github.workspace }}/workdir/openwrt
          make -j$(nproc) || make -j1 || make -j1 V=s
          echo "status=success" >> $GITHUB_OUTPUT

      - name: Organize firmware
        id: organize
        if: steps.compile.outputs.status == 'success'
        run: |
          cd ${{ github.workspace }}/workdir/openwrt/bin/targets/*/*
          rm -rf packages *.buildinfo profiles.json sha256sums *.vmdk *.img.gz *.bin *.manifest
          mv *.tar.gz $GITHUB_WORKSPACE/${{ matrix.target.branch }}-${{ env.FILE_DATE2 }}.tar.gz
          echo "FIRMWARE=$GITHUB_WORKSPACE" >> $GITHUB_ENV
          echo "status=success" >> $GITHUB_OUTPUT

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        if: steps.organize.outputs.status == 'success'
        with:
          name: Firmware-${{ matrix.target.branch }}-${{ env.FILE_DATE2 }}
          path: ${{ env.FIRMWARE }}/*.tar.gz

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Combine all version info
        run: |
          cat *.version.txt > version_body.txt
          echo "ALL_VERSION_INFO<<EOF" >> $GITHUB_ENV
          cat version_body.txt >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV

      - name: Create release
        if: ${{ env.UPLOAD_RELEASE == 'true' }}
        uses: ncipollo/release-action@main
        with:
          name: R${{ env.DATE1 }} for ${{ env.PRODUCT_NAME }}
          allowUpdates: true
          tag: ${{ env.PRODUCT_NAME }}
          commit: ${{ github.sha }}
          token: ${{ secrets.TOKEN3 }}
          body: |
            **OpenWRT official Mini Firmware for x86/64**

            ${{ env.ALL_VERSION_INFO }}

      - name: Upload to GitHub Release Assets
        if: env.UPLOAD_RELEASE == 'true'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ env.PRODUCT_NAME }}
          files: |
            ${{ github.workspace }}/*.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.TOKEN3 }}

      - name: Telegram notification
        run: |
          curl "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" -d "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}&text=✔️ ${{ env.PRODUCT_NAME }} 固件已成功编译！\n⏰ 时间: ${{ env.DATE1 }}\n📅 下载地址: ${{ env.GITHUB_RELEASE }}" >/dev/null 2>&1
